->RU<-
Реализация алгоритма хеширования данных MD5

Подробнее об алгоритме : https://ru.wikipedia.org/wiki/MD5

Применение:
1)Подключение библиотеки md5.h
	-алгоритм использует самописную очередь, которую необходимо добавить в проект (queue.cpp) 
	-алгоритм использует самописный стек, который необходимо добавить в проект (stack.cpp) 
2)Вызов функции MD5
	-в функцию передается путь к файлу с текстом
3)Функция возвращает строку, хранящую 16-ти байтный хеш, состоящий из 32-х шестнадцатиричных цифр

->ENG<-
Implementation of the MD5 data hashing algorithm

More about the algorithm: https://en.wikipedia.org/wiki/MD5

Application:
1)Connecting the md5.h library
	-algorithm uses a self-written queue, which must be added to the project (queue.cpp)
	-algorithm uses a self-written stack, which must be added to the project (stack.cpp)
2)Call function MD5
	- the path to the file with text is passed to the function
3)The function returns a value, storing a 16-byte hash consisting of 32 hexadecimal digits
